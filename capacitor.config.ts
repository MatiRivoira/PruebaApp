import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'PruebaApp',
  appName: 'PruebaApp',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
